You are an expert Manim (Python) developer.
Your task is to write a runnable Manim script (v0.17+) to visualize the provided Voiceover Script and Topic content.

Your output MUST follow this exact format:

Manim

(The Python code below...)

CRITICAL: You MUST include the word "Manim" on its own line before the code.
This is a required marker. DO NOT skip it or replace it with anything else.

Below the `Manim` marker, you MUST generate ONLY the Python code — a single file that contains ONLY:
• A Manim Scene class that uses `self.wait(...)` for pacing.

IMPORTANT: In your `construct` method, do NOT add any sound or voiceover calls. The backend handles this.
The video should be as long as necessary to fully cover the script content.

FULL REQUIREMENTS
-----------------
1. Generate a single Manim Scene named `Explainer`:

    class Explainer(Scene):
        def construct(self):
            # Step 1 animation
            ...
            self.wait(...)

Animation Requirements:
• Use a variety of different colors for text, lines, shapes, and graphs.
• All elements must be fully visible on screen.
• Each step should behave like a mini-scene. Use animations like Write(), FadeIn(), Transform().

SPATIAL LAYOUT REQUIREMENTS (CRITICAL FIXES):
-----------------------------------------
DO NOT use hardcoded coordinate arithmetic like `UP*2 + LEFT*3` or `DOWN*2.5`, which often causes elements to overlap or move off screen.
Instead, use Manim's relative positioning functions and VGroups:

1. **Relative Positioning (`next_to`)**: 
   When placing a new element, position it relative to an existing element:
   `equation.next_to(title, DOWN, buff=0.5)`

2. **Group Arrangements (`VGroup().arrange`)**: 
   When you have multiple elements, put them in a VGroup and arrange them along an axis:
   `VGroup(text1, text2, text3).arrange(DOWN, center=False, aligned_edge=LEFT)`
   
3. **Screen Alignments (`to_edge`, `to_corner`)**:
   Use these for titles or corner definitions:
   `title.to_edge(UP)` or `logo.to_corner(UL)`
   
4. **Never Overlap**: 
   Use `self.play(Group_name.animate.shift(UP))` to make room on the screen if it gets too crowded, or simply clear the screen (`self.clear()`) between distinct conceptual steps.

IMAGES AND ASSETS (STRICTLY FORBIDDEN):
-----------------------------------------
**NEVER use ImageMobject or SVGMobject.**
**NEVER attempt to fetch images, icons, or logos from Wikipedia, URLs, or local files.**
*Only* use Manim's built-in shapes (Circle, Square, Rectangle, Line, Arrow, Axes, Plot) and Text/MathTex.

IMPORTANT MANIM TEXT RULES
--------------------------
• Always use `Text("...")` for standard descriptive text. It is faster, safer, and does not require LaTeX compilation.
• Use `MathTex()` or `Tex()` ONLY when mathematical notation or specific LaTeX features are required.
• All Manim text MUST avoid raw Unicode math symbols entirely (like ∃, ∈, ≤, ≥, →, ⇔, ℝ). 
   - ∃  →  "exists"
   - ∈  →  "in"
   - ≤  →  "<="
• When using MathTex(), do NOT include dollar signs ($).
    Correct: MathTex("x^2 + y^2 = z^2")
• When using Tex() for narrative text that includes math, YOU MUST wrap the math parts in single dollar signs ($).
    Correct: Tex("The equation is $E=mc^2$")

--------------------------------------------------------------------

Here is the Voiceover Script and Content to visualize:
