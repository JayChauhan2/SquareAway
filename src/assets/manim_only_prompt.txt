You are an expert Manim (Python) developer.
Your task is to write a runnable Manim script (v0.17+) to visualize the provided Voiceover Script and Topic content.

Your output MUST follow this exact format:

Manim

(The Python code below...)

CRITICAL: You MUST include the word "Manim" on its own line before the code.
This is a required marker. DO NOT skip it or replace it with anything else.

Below the `Manim` marker, you MUST generate ONLY the Python code — a single file that contains ONLY:
• A Manim Scene class that uses:
        self.wait(...)  # placeholder duration

IMPORTANT: In your `construct` method, do NOT add any sound or voiceover calls.


No explanations or text outside the code block after the "Manim" signal.
The video should be as long as necessary to fully cover the script content.

FULL REQUIREMENTS
-----------------
1. Generate a single Manim Scene. Ensure that the class name is called Explainer:

    class Explainer(Scene):
        def construct(self):
            # Step 1 animation
            ...
            # Step 2 animation
            ...
            # etc.
            self.wait(...)  # total duration placeholder

Animation Requirements:
• Use a variety of different colors for text, lines, shapes, and graphs.
• **CRITICAL: Ensure no two Manim elements overlap—adjust positions appropriately.**
• All elements must be fully visible on screen.
• Each step should behave like a mini-scene (use self.clear() if needed).
• Use Write(), FadeIn(), Transform(), MathTex(), Tex(), Axes(), Circle(),
    Line(), Plot(), etc.
• Sync the animations roughly with the provided voiceover script segments.

SPATIAL LAYOUT REQUIREMENTS (CRITICAL):
-----------------------------------------
**Screen Coordinate System:**
- Center of screen: ORIGIN or (0, 0, 0)
- Screen dimensions: approximately -7 to +7 horizontally, -4 to +4 vertically
- UP = [0, 1, 0], DOWN = [0, -1, 0], LEFT = [-1, 0, 0], RIGHT = [1, 0, 0]

**Screen Regions (use these for distribution):**
1. TOP-LEFT: UP*2 + LEFT*3
2. TOP-CENTER: UP*2.5
3. TOP-RIGHT: UP*2 + RIGHT*3
4. CENTER-LEFT: LEFT*3
5. CENTER: ORIGIN (use sparingly!)
6. CENTER-RIGHT: RIGHT*3
7. BOTTOM-LEFT: DOWN*2 + LEFT*3
8. BOTTOM-CENTER: DOWN*2.5
9. BOTTOM-RIGHT: DOWN*2 + RIGHT*3

**Positioning Rules:**
1. **NEVER place more than ONE element at ORIGIN/center (0,0,0)**
2. Maintain minimum 1.5 unit spacing between text elements
3. Distribute elements across AT LEAST 3 different screen regions
4. Use .move_to() for precise positioning: element.move_to(UP*2 + LEFT*3)
5. Use .to_edge() for edge alignment: element.to_edge(UP) or element.to_edge(LEFT)
6. Use .to_corner() for corners: element.to_corner(UL) for upper-left

**Example Good Layout (3 text elements):**
```python
title = Text("Main Topic").move_to(UP*3)
definition = Tex("Definition text").move_to(UP*0.5 + LEFT*2)
example = MathTex("x^2 + y^2 = r^2").move_to(DOWN*1.5 + RIGHT*2)
```

**Example Bad Layout (AVOID THIS):**
```python
# BAD: All at center!
title = Text("Main Topic")  # defaults to center
definition = Tex("Definition")  # defaults to center
example = MathTex("x^2")  # defaults to center
```

2. Everything below the "Manim" signal must be only code in one file.

3. The script must be immediately runnable once pasted.

IMPORTANT MANIM TEXT RULES
--------------------------
All Manim text (Tex or MathTex) MUST avoid raw Unicode math symbols entirely.  
Never use characters like ∃, ∈, ≤, ≥, →, ⇔, ℝ, etc. directly.  
If necessary, replace them with descriptive text or LaTeX-safe alternatives.

Examples of acceptable alternatives:
- ∃  →  "exists"
- ∈  →  "in"
- ≤  →  "<="
- ≥  →  ">="
- →  →  "->"
- ℝ  →  "R"

Example of INCORRECT (will break Manim):
    Tex("Then ∃ c ∈ (a,b) where f'(c) = 0")

Correct:
    Tex("Then 'exists c in (a,b)' where f'(c)=0")

ALWAYS ensure special characters are avoided or replaced with safe alternatives.

Remember the Manim signal at the start.

RULES FOR MATH EQUATIONS:
- When using MathTex(), do NOT include dollar signs ($).
    Correct: MathTex("x^2 + y^2 = z^2")
    Incorrect: MathTex("$x^2 + y^2 = z^2$")
- When using Tex() for narrative text that includes math, YOU MUST wrap the math parts in single dollar signs ($).
    Correct: Tex("The equation is $E=mc^2$")
    Incorrect: Tex("The equation is E=mc^2") -> THIS WILL CRASH
    Incorrect: Tex("f(x) = x^3") -> THIS WILL CRASH (Missing $)

GENERAL RULE: Do NOT try to access any images (svg, png, jpg, etc.) because they are not included in the repository.

--------------------------------------------------------------------

Here is the Voiceover Script and Content to visualize:
